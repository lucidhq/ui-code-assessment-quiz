import { css } from '@emotion/core'

export const radio = css({
  fontSize: 20,
  marginBottom: 5,
})

export const input = css({
  marginRight: 10,
  transform: 'scale(1.5)',
  cursor: 'pointer',
})
