import { css } from '@emotion/core'

export const flex = css({
  flexGrow: 1,
  flexShrink: 1,
  flexBasis: 'auto',
  marginBottom: 20,
})

export const text = css({
  fontSize: 25,
  textAlign: 'left',
})
