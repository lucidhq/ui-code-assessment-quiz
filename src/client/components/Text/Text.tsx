import React from 'react';

export const Text = (question: object) => {

  const q = 'Igneous rocks are formed by excessive heat and pressure.';

  // make answer case insensitive

  const correct_answer = false;

  return (
    <div className="text">
    </div>
  );
};